---

- name: include variables
  include_vars: "{{lookup('first_found', params)}}"
  vars:
    params:
      files:
        - "vars/{{ansible_distribution_major_version}}.yml"
        - 'vars/main.yml'
  tags:
    - bc

- name: remove package bc
  package:
    state: 'absent'
    name: "{{package_name_bc}}"
    install_recommends: 'no'
  tags:
    - bc
