---

- name: import tasks verify
  import_tasks: verify.yml
  when: not (pod_registry_state is match('false|no|remove|quiesce|inactive'))
  tags:
    - pod_registry

- name: import tasks install
  import_tasks: install.yml
  when: not (pod_registry_state is match('false|no|remove|quiesce|inactive'))
  tags:
    - pod_registry

- name: import tasks enable
  import_tasks: enable.yml
  when: (pod_registry_state is match('start|on|enable'))
  tags:
    - pod_registry

- name: import tasks disable
  import_tasks: disable.yml
  when: (pod_registry_state is match('stop|off|disable'))
  tags:
    - pod_registry

- name: import tasks remove
  import_tasks: remove.yml
  when: (pod_registry_state is match('false|no|remove'))
  tags:
    - pod_registry
