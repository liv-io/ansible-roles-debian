---

dependencies:
  - {role: podman, when: not (pod_registry_state is match('false|no|remove|quiesce|inactive'))}
  - {role: apache2_utils, when: not (pod_registry_state is match('false|no|remove|quiesce|inactive'))}
