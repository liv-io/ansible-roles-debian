---

- name: import tasks install
  import_tasks: install.yml
  when: not (fuse_overlayfs_state is match('false|no|remove|quiesce|inactive'))
  tags:
    - fuse_overlayfs

- name: import tasks remove
  import_tasks: remove.yml
  when: (fuse_overlayfs_state is match('false|no|remove'))
  tags:
    - fuse_overlayfs
