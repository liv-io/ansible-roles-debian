---

- name: import tasks install
  import_tasks: install.yml
  when: not (lowdown_state is match('false|no|remove|quiesce|inactive'))
  tags:
    - lowdown

- name: import tasks remove
  import_tasks: remove.yml
  when: (lowdown_state is match('false|no|remove'))
  tags:
    - lowdown
