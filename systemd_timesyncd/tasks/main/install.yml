---

- name: include variables
  include_vars: "{{lookup('first_found', params)}}"
  vars:
    params:
      files:
        - "vars/{{ansible_distribution_major_version}}.yml"
        - 'vars/main.yml'
  tags:
    - systemd_timesyncd

- name: install package mailx
  package:
    state: 'present'
    name: "{{package_name_mailx}}"
    install_recommends: 'no'
  when: (systemd_timesyncd_monitor_mail_state is match('true|yes|enable'))
  tags:
    - systemd_timesyncd

- name: install package systemd-timesyncd
  package:
    state: 'present'
    name: "{{package_name_systemd_timesyncd}}"
    install_recommends: 'no'
  tags:
    - systemd_timesyncd

- name: stat file monit.d
  stat:
    path: "{{file_dst_monit_d}}"
  register: register_monit_d
  when: (systemd_timesyncd_monitor_monit_state is match('true|yes|enable'))
  tags:
    - systemd_timesyncd

- name: create file monit.d
  file:
    state: 'directory'
    path: "{{file_dst_monit_d}}"
    mode: "{{file_mode_monit_d}}"
    owner: "{{file_owner_monit_d}}"
    group: "{{file_group_monit_d}}"
  when: (systemd_timesyncd_monitor_monit_state is match('true|yes|enable')) and (register_monit_d.stat.exists == False)
  tags:
    - systemd_timesyncd

- name: check service monit
  shell: "{{command_cmd_check_service_monit}}"
  register: register_check_service_monit
  changed_when: (register_check_service_monit.rc > 1)
  failed_when: (register_check_service_monit.rc > 1)
  check_mode: 'no'
  when: (systemd_timesyncd_monitor_monit_state is match('true|yes|enable'))
  tags:
    - systemd_timesyncd

- name: stat file nftables.d
  stat:
    path: "{{file_dst_nftables_d}}"
  register: register_nftables_d
  when: (systemd_timesyncd_nftables_state is match('true|yes|enable'))
  tags:
    - systemd_timesyncd

- name: create file nftables.d
  file:
    state: 'directory'
    path: "{{file_dst_nftables_d}}"
    mode: "{{file_mode_nftables_d}}"
    owner: "{{file_owner_nftables_d}}"
    group: "{{file_group_nftables_d}}"
  when: (systemd_timesyncd_nftables_state is match('true|yes|enable')) and (register_nftables_d.stat.exists == False)
  tags:
    - systemd_timesyncd

- name: check service nftables
  shell: "{{command_cmd_check_service_nftables}}"
  register: register_check_service_nftables
  changed_when: (register_check_service_nftables.rc > 1)
  failed_when: (register_check_service_nftables.rc > 1)
  check_mode: 'no'
  when: (systemd_timesyncd_nftables_state is match('true|yes|enable'))
  tags:
    - systemd_timesyncd
